# CLAUDE.md — {{ hostname }}

## Identité
| | |
|---|---|
| Hostname | `{{ hostname }}` |
| OS | {{ os_pretty }} |
| Kernel | `{{ kernel }}` |
| Architecture | {{ arch }} |
| CPU | {{ cpu_count }} cores |
| RAM | {{ ram_total }} |
| Disque(s) | {{ disks }} |
| IP locale | `{{ local_ip }}` |
| IP Tailscale (si Tailscale installé) | `{{ tailscale_ip|default("non installé") }}` |

## Rôle
{{ machine_role|default("À compléter par l'utilisateur. Exemples : serveur d'app perso / NAS familial / VPS de test / poste de travail / etc.") }}

## Services qui tournent
{% if services %}
{% for s in services %}
- `{{ s.unit }}` — {{ s.description }}
{% endfor %}
{% else %}
(Aucun service notable détecté à l'onboarding. À compléter au fil de l'usage.)
{% endif %}

## Skills installés
{% for skill in installed_skills %}
- `{{ skill }}`
{% endfor %}

## Convention de travail
- Pas de secrets en clair dans le chat (voir `feedback_no_secrets.md` dans la mémoire).
- Validation manuelle de chaque appel d'outil (voir `feedback_no_auto_authorize.md`).
- Documenter au fur et à mesure les découvertes (voir `feedback_doc_as_you_go.md`).
- Vérifier les binds réseau après chaque install (voir `feedback_check_network_binds.md`).

## Langue
{{ user_language|default("fr") }}

## Origine
Généré par `claude-fleet-starter` lors de l'onboarding initial, le {{ date }}.
À enrichir au fil de l'usage.
